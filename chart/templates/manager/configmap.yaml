apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: kdex-nexus-controller-manager
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
